function devidor(n){
  var res = "";
  for(var i = 1;i <= n;i++){
    if((i % 105) === 0){
      res += i + " ";
    }
    if((i % (630) === 0)){
      res += "\n";
    }
  }
  return res;
}
function devidor_eff(n){
  var str = "";
  var index = Math.floor(n / 105);
  for(var i = 1;i <= index;i++){
    var tmp = i * 105;
    str += tmp + " ";
    if((i % 6) === 0) str += "\n";
  }
  return str;
}
console.log(devidor_eff(2000));
